# Demo build config. `vite build --mode demo` loads this file, turning on the
# in-memory mock (sample data, no backend, no connection to the real Supabase).
# Safe to deploy publicly — see src/lib/demoClient.js.
VITE_DEMO_MODE=true
