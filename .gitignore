node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.vite/

# Claude Code: per-user / per-machine config, not shared.
.claude/settings.local.json

# scripts/ — one-off snapshots and intermediate outputs from the SDS-link
# pipeline. The .mjs files are reusable tooling; the .json files are local
# state regenerated on each run.
scripts/db-items.json
scripts/sds-links-from-file2.json
scripts/sds-link-matches.json
scripts/sds-link-verdicts.json
scripts/sds-link-apply.json
scripts/sds-mismatch-report.json
sds-check.pdf
